# M2 手工验收清单（npm run tauri dev 逐项勾选）

- [ ] 首次启动：选择工作区后进入文件库，列表为空有中文引导
- [ ] 新建：输入中文名创建成功；非法名（a/b、空）出现中文错误提示
- [ ] 编辑：中文输入法录入 Tab/Enter/Delete、双击编辑、拖拽节点、折叠/展开、Ctrl+滚轮缩放画布（注意：默认滚轮是平移）、画布平移
- [ ] 保存：Ctrl+S 后工作区出现 .md 与 .zen.json；.md 内容可直接粘贴给 AI
- [ ] 自动保存：编辑后静置 5 秒，直接关闭应用，重开内容未丢
- [ ] 重开：折叠状态恢复
- [ ] 外部编辑：用记事本改 .md 增加一个二级标题，返回文件库重开导图，新节点出现
- [ ] 删除：确认后文件进入回收站
- [ ] 工作区无 .tmp 残留文件（保存/取消过程中）
- [ ] Windows 下重命名覆盖已存在文件正常（原子写 rename 路径）
- [ ] Windows 高分屏（150%/200% 缩放）显示正常
- [ ] 500+ 节点大图（手工快速 Tab 扩展）无肉眼卡顿
- [ ] 真机验证：错误面板"以纯文本打开修复"按钮能调起系统默认编辑器

## M3 验收（v0.3.0）

- [ ] 复制：无选中 Ctrl+Shift+C = 整图；选中节点后 = 该分支（H1 重计）；粘贴给任意编辑器核对
- [ ] 多行粘贴：编辑节点时粘贴 3 行文本 → 首行入当前节点、后两行成子节点
- [ ] 关闭守卫：编辑后直接点窗口 × → 三态各试一遍（保存关闭/放弃/取消）
- [ ] 保存失败：占用 .md 文件（如用 Word 打开锁定）后 Ctrl+S → 横幅报错、返回不离开
- [ ] 忽略块：用记事本往 .md 里加一段文字 → 打开有横幅 → 保存时确认丢弃
- [ ] 导入：选一个含段落的外部 .md → 预览提示 → 确认入库 → 原文件未动
- [ ] 布局：三布局切换即时重排，重开恢复；切换不出现 ● 脏标记
- [ ] 验收增补①：窗口最大化/还原，画布随窗口大小变化（无固定尺寸残留）
- [ ] 验收增补②：工具栏「居中」按钮 → 缩放回 100% 且导图回到画布中心
- [ ] 验收增补③：工具栏 −/＋ 缩放、Ctrl+滚轮缩放；「根居中」保持缩放回根；「适配」全图完整可见居中
- [ ] 验收增补④：滚动条只出现在文件库列表内（画布区无滚动条；工具栏右侧无滚动条）
- [ ] 验收增补⑤：应用启动窗口居于屏幕中心
- [ ] 验收增补⑥：切到某布局后新建导图默认即该布局（记住偏好）；无 sidecar 的外部 .md 打开也用偏好布局

## M4 验收（v0.4.0）

- [ ] 双主题：右下角循环切换 自动/纸/夜墨；界面与画布同步；跟随系统开关系统暗色验证
- [ ] 砚栏：静置 35% 淡化、悬停/Tab 聚焦浮现；全部图标按钮 title 提示与快捷键
- [ ] 画布纸墨：根=墨块、一级=墨青浅底、连线曲线淡墨、选中=朱砂环；夜墨反之月白根
- [ ] 文件库：卡片网格、.md 等宽徽章、悬停出重命名/删除、空态朱砂印引导
- [ ] 对话框：四种对话框风格统一；Esc 关闭；守卫与忽略确认不同时出现
- [ ] 印记：保存成功右上朱砂"已存"闪光；复制"已复制"墨青印；未保存 ● 为朱砂方点
- [ ] 系统开"减少动态效果"后砚栏/印章无动画
- [ ] 暗色下走查对比度：正文/次要文字/按钮/横幅/节点文字

## M5a 验收（v0.5.0）

- [ ] 目录树折叠：左栏 ‹/› 折叠成窄条再展开，卡片区自适应占满；等宽小字缩进不突兀
- [ ] 过滤：点目录节点仅显示该层卡片（不含子目录）；「全部」恢复全量且卡片附所在层小字
- [ ] 移动：卡片「移动」→ 对话框选目录（当前层禁用）→ 确认后 .md 与 .zen.json 同入新层，列表与左树即时刷新；对话框内联新建目录后取消，目录仍出现在左树
- [ ] 新建目录：左树「新建目录」在选中层下建立；非法名（含 \ / : * ? " < > |）中文报错
- [ ] 空态：空目录显示「这一层还没有导图」；空工作区仍显示朱砂印引导且可先建目录
- [ ] 视觉降级：页首标题 18px、卡片 gap 收敛、仅新建/导入为实心主按钮（其余文字链接化），两主题下走查
- [ ] 纸面/案头文案：界面与提示统一「案头」（列表页）「纸面」（画布），无「文件库」等旧称残留
- [ ] 同目录守卫：对某层导图发起移动，当前层目标项禁用不可选；构造同目录调用导图不被误改名（时间戳后缀）

## M5b 验收（v0.6.0）

- [ ] 节点备注：选中节点 → 砚栏备注钮打开对话框 → 编辑保存后节点出现角标，悬停显示备注；清空保存角标消失；保存后 .md 中备注为节点标题下的 `> ` 引用块行；重开导图备注与角标俱在
- [ ] 节点连线：节点文本含 `[[名称]]` 时保存后出现到目标节点的关联线（唯一命中才建线）；重开导图连线复现；.md 中只有纯文本标记；纸/夜墨双主题下连线颜色走查
- [ ] 设置页：案头页首设置入口 → 两开关（复制含备注/保留双链标记）切换后重开应用仍记住；复制 md 按开关生效
- [ ] 导出 PNG：砚栏导出钮 → 三入口对话框 → 导出 PNG → 系统保存对话框默认文件名「导图名.png」→ 保存后目标路径出现可打开的 PNG 图片，右上「已存」印记
- [ ] 导出 SVG：同上入口选导出 SVG → 文件为可编辑矢量（文本编辑器打开含 svg 标题）
- [ ] 复制为图片：三入口选复制为图片 → 到任意编辑器粘贴出整图 PNG，右上「已复制」印记
- [ ] 导出取消：保存对话框点取消 → 无文件产生、无印记、无报错
- [ ] 导出失败：目标目录无写权限时 → 顶部横幅中文报错（不静默）

## M5d 验收（v0.7.0）

- [ ] 开屏视觉：删配置首次启动（或换无工作区文件夹）→ 居中朱砂方印 48px + 「Mind Map Zen」+「想法落成 .md」+ 主按钮「创建工作区」/次链接「选择已有文件夹」，页首栏隐藏；点主按钮选文件夹后进案头
- [ ] 三区布局：工具栏（左小印+名，右设置齿轮/导入/新建带字）+ 左目录树 + 中卡片网格 + 右大纲预览；树折叠后卡片区自适应占满；工作区路径在树根 tooltip
- [ ] 树图标：文件夹 IconFolder、导图文件行 IconFile（名不含 .md 扩展）；「全部」根节点显示工作区名
- [ ] 预览排版：单击卡片/树文件 → 选中高亮 + 右侧大纲树（等宽字、层级缩进、备注行尾 ✎ 角标、折叠态 …）；双击或「打开」按钮进纸面；解析失败显示「无法预览」
- [ ] 双击手感：卡片/树文件单击只选中不打开，双击打开不过灵敏（无单击误入纸面）
- [ ] 连线净化：浮动条连线按钮画线后画布节点文本**无 [[ ]] 标记**（显示文本全程不动）；Ctrl+S 后 .md 节点句尾含 `[[名]]` 标记；重开导图连线复现且文本仍纯净
- [ ] 弯曲记忆：点选连线拖控制点弯出弧度 → 静置 5s 自动保存 → 关闭重开导图弧度恢复；同一节点多条连线部分拖弯时未拖弯的线仍走默认 S 曲线（且其控制点可继续拖动不报错）
- [ ] 弯曲失联语义：拖弯保存后重命名源/目标节点 → 静置自动保存后再重开弯曲保持（路径对键随改名迁移）；改名后**放弃保存**直接重开 → 弯曲回落默认曲线（旧键失联丢弃，sidecar 级语义）
- [ ] 工作区切换入口：设置对话框「更换工作区」→ 选新文件夹 → 案头卡片/左树刷新、选中预览清空；取消选择则停留原地

## M5c 验收（v1.0.0）

- [ ] 应用图标：窗口标题栏/任务栏显示朱砂方印（#B5453C 印底 + 白色导图分形），不再是 Tauri 默认图标
- [ ] 浮签提示：案头与纸面全部图标按钮悬停出现反白小浮签（ZenTooltip），延迟/样式统一，无原生 title 双提示
- [ ] 迁移后首启：从旧版（com.tauri.dev 目录已有 config.json）升级首启 → 工作区/设置原样保留；%APPDATA% 下 com.mindmapzen.app 目录出现 config.json 与 .migrated-from-tauri-dev 留档标记，旧目录原样未动
- [ ] 双主题走查：纸墨/夜墨下走查令牌化后的间距/字号（案头三区、对话框、砚栏、题签、印记）无错位突兀


## M12a 验收（v1.2.0）

- [ ] 方向键手感：点选节点后上下左右在节点间按几何最近移动选中，目标节点居中并带选中环；无选中时按任意方向键聚焦根；Shift/Ctrl+方向组合不被劫持（Ctrl+上下仍是画布缩放）
- [ ] 编辑框内方向键：双击节点进入编辑后按四方向只移动光标（不跳节点、不关编辑框）；框外 Tab 建子 / Enter 建同级 / Delete 删节点语义与 v1.1 一致
- [ ] Tailwind 与旧样式并存：案头/纸面/对话框/印记在双主题下与 v1.1.1 逐屏比对无视觉差异、无闪烁错位（零视觉变更验收线）；ui/ 九组件未接入页面

## M12b 验收（v1.3.0，青松工作台）

> 已被 M14 节取代（官方值优先）。

- [ ] 双主题走查：晨松/夜航下案头三区、纸面、对话框、浮签、印记逐屏走查，令牌色一致，无旧纸墨/夜墨色残留（含 prefollow auto 跟随系统）
- [ ] 案头三区：命令栏 48px（左面包屑印章+工作区名，右图标钮 32px 等距）/ 左树 240px 行高 32px（可折叠）/ 右预览 280px 档案卡；卡片 8px 圆角、hover 青松边、选中浅底描边
- [ ] 纸面停泊栏：底部居中 40px、静置全不透明（旧「静置淡化悬停浮现」已退役）；按钮 32px；布局切换组点亮态青松浅底；分隔线与边框走查
- [ ] 纸面题签与脏印：左下导图名等宽字体，未保存时缀朱砂方点；右下主题钮 32px
- [ ] 引擎青松双色：根=石墨实心亮字、一级=青松浅底青松细边、一般节点=白底灰边、选中=青松环、连线雾灰曲线；夜航对应反转（月白根/深底一级）双主题走查
- [ ] 开屏：朱砂方印 48px + 「Mind Map Zen」+ 主钮「创建工作区」青松极简（无工作区首启态）
- [ ] 对话框与浮签全面 ui/*：七对话框（新建/重命名/新建目录/删除/移动/导入预览/设置 + 纸面四框）Esc/✕/遮罩关闭语义不变，动作行右对齐；图标钮悬停浮签青松皮肤；移动对话框内目录树与案头左树同皮肤（32px 行、等宽小字、当前层禁用灰）
- [ ] 交互语义零变更复查：testid/快捷键/单击双击/自动保存肌肉记忆与 v1.2 一致（E2E 零适配全绿为证）


## M14 验收（v1.4.0，shadcn 组件制）

- [ ] 案头侧栏折叠：SidebarTrigger 折叠成图标栏再展开；Ctrl+B 键盘折叠/展开；重启应用折叠态经 cookie（sidebar_state，7 天）保持
- [ ] 激活态走查：选中卡片/树行 isActive 高亮随选择即时迁移；折叠态悬停菜单项出浮签
- [ ] 三页走查：开屏认证卡（w-96 居中、主钮通栏）、案头（Sidebar+卡片网格+预览卡）、纸面（停泊栏全 ui 件）间距/圆角与 shadcn 官方一致
- [ ] 官方值抽检：浮签内距 6px 12px、对话框内距 24px、侧栏宽 256px、卡片圆角 12px（--radius 0.5rem + 4px）——E2E 视觉冒烟 4 已锁
- [ ] 对话框宽度官方化 360→512px（sm:max-w-lg），九个对话框逐个走查布局与滚动
- [ ] 纸面细节：命令栏按钮悬停浮签内距正常（曾因未分层通配复位全 app padding 工具类失效，M14 已根治）；布局组三键点亮态与切换正常
- [ ] 双主题：夜航下三页+对话框+浮签+侧栏对比度与色一致走查

## M14b 验收（线条→区块精修）

- [ ] 案头区块化：左侧为 bg-sidebar 色场（侧栏无边框线、四周留悬浮呼吸位），右侧内容为圆角浮层（rounded-xl + 阴影）；页首与主区之间无 border-b 横线，分区由浮层圆角边界承担
- [ ] 卡片区块化：悬停时升影浮起（无青松描边线），选中为薄荷色块 + 60% 描边收口；卡片网格与右列预览形成「白卡浮起 / muted 静音面板下沉」两种区块语义
- [ ] 开屏：页面自上而下轻渐变（背景色渐入 muted），白卡浮于其上；双主题各走查一遍
- [ ] 纸面浮层：命令栏与节点操作条无描边线，靠阴影托浮（圆角收编官方阶梯 8px）
- [ ] 夜航层次：卡片亮于内容场（--muted 校准 #15191B 后，暗下卡与场、静音面板、骨架屏均有层次）
- [ ] 错误横幅/忽略块横幅/原始内容块：纯色块无描边线（晨松/夜航各一遍）

## M15 验收（案头文件化，v1.5.0）

- [ ] 页首底线已去：inset 圆角浮层内页首与主区之间无线条（双主题各一遍）
- [ ] 左树文件树：目录行尾箭头旋入动画（200ms）；点箭头折叠/展开子树；点行面选中目录（不折叠）；树根（工作区名）可收起全树；空目录无死箭头；默认全展开
- [ ] 三态切换：进案头=空态引导（从左侧选择目录或导图）；点目录/文件夹 tile=资源管理器大图标视图；点导图 tile/树文件行=详情态；详情态「返回目录」回资源管理器
- [ ] 资源管理器：文件夹 tile（青松大图标）单击=左树联动选中；导图 tile 显示创建日期+大小（等宽小字）；悬停浮现移动/重命名/删除三钮；双击打开（单击后 220ms 内双击仍能打开）
- [ ] 文件详情：摘要条（名称/所在层/大小/创建/修改 + 返回/打开）完整；下层 markdown 预览真实渲染（标题层级/引用块 muted 色块+青松左条/列表/表格）；[[..]] 连线标记不显示
- [ ] 元数据：新建/导入/移动后 tile 的创建时间与大小即时正确；工作区切换回空态引导
- [ ] E2E 回归：35 用例零适配语义全绿（含三态/折叠扳机/移动流新断言）

## M16 验收（导图模板，v1.6.0）

- [ ] 新建对话框：名称输入 + 模板选择（Select，内置两项居首 + 工作区 templates/ 用户模板）；默认空白，直接输名称回车/确认 = 旧行为
- [ ] 「软件开发跟踪」实例化：根节点 = 输入名（模板根「项目名」占位被替换），迭代四态/Bug/想法池/决策记录结构完整；画布与详情态 md 预览层级渲染正确
- [ ] 用户模板：templates/ 放 .md 即出现在列表（含子目录）；编辑模板文件 = 编辑普通导图（目录在案头正常可见）
- [ ] 非法模板（无标题文本）不阻断创建（回退空白）

## M17 验收（mermaid 备注即宿主，v1.7.0）

- [ ] 详情态预览：备注引用块内 ```mermaid 围栏渲染成 SVG 小图（居中、随容器收宽）；普通代码块仍是等宽源码
- [ ] 双主题：晨松 default / 夜航 dark，切换主题后图重渲染
- [ ] 语法错误降级：错误摘要（朱砂首行）+ 源码保底显示，不白屏
- [ ] md 事实源：编辑器加备注（Shift+F2）粘贴 mermaid 源，保存重开围栏逐字保留（roundtrip）
- [ ] AI 写入：外部改 .md 添加 mermaid 备注，应用重开即渲染
- [ ] 性能：无 mermaid 的导图预览不加载 mermaid 库（懒加载分块）

## M18 验收（节点图标，v1.8.0）

- [ ] 画布图标渲染：含 ::flag 标记的节点显示 lucide 图标（节点内容区，随主题配色）
- [ ] 图标管理器：选中节点 → 浮动条笑脸钮 → 精选 64 网格；点选 toggle（青松环高亮）；保存生效
- [ ] 全集搜索：输入名字或语义标签（如「时间」「warning」）懒加载 lucide 全集（2048），非精选图标可选中并即时注册
- [ ] md 事实源：落盘句尾 ::name；AI/手写改 .md 重开即生效；详情态预览与大标题文本均不显示标记
- [ ] 撤销重做覆盖图标变更（SET_NODE_ICON 入历史）；删除图标后标记消失
- [ ] 双主题走查图标着色（currentColor 随节点文字色）

## M19 验收（节点插图，v1.9.0）

- [ ] 选图流：节点浮动条图片钮 → 对话框（未设置占位/muted 预览底）→ 选择图片（Tauri 对话框，png/jpg/gif/webp/bmp）→ 预览即时更新 + 画布渲染
- [ ] 画布图片：dataURL 渲染（免 asset 协议），按主题上限等比缩放；重开导图图片恢复
- [ ] md 事实源：落盘行尾 ![alt](assets/文件名.png)；图片文件复制入工作区 assets/（同名防撞 -N 后缀）；详情态预览原生渲染图片
- [ ] 手写/AI：直接改 md 加 ![..](..) 标记重开即生效；文件缺失节点宽容不渲图（对话框显示「图片文件不可读」），md 标记保留
- [ ] 移除插图：对话框「移除」→ 画布消失、md 标记消失（assets 文件保留——引用可能复用）
- [ ] 撤销重做覆盖插图变更（SET_NODE_IMAGE 入历史）

## M20 验收（版本管理，v2.0.0）

- [ ] 启用：设置 → 版本管理 → 勾选「版本管理」→ 工作区出现 .git 仓库（首次自动 init）
- [ ] 自动备份：编辑导图保存后等 10 分钟（或重启设置开关触发即备）→ git log 出现「自动备份 · 时间 · N 文件变更」；无变更不产生空提交
- [ ] 远程备份：填 HTTPS 地址 + PAT → 备份后远端出现提交（zen-origin 指向配置地址）；推送失败时状态行显示原因（下次重试），本地提交不受影响
- [ ] 设置页状态行：最近提交摘要 + 未推送数 + 最近备份结果；「立即备份」手动触发
- [ ] git 不可用的机器（未装 git）：状态行显示「备份失败：git init 失败…」，应用不崩
- [ ] 仓库可用任意 git 工具检查（.git 在工作区根，标准仓库）

## v2.1 验收（AI 协作开发模板）

- [ ] 新建对话框模板列表：「空白导图」+「AI 协作开发」（旧「软件开发跟踪」退役）
- [ ] 实例化：根名替换；结构含 目标/想法池/进行中/待我验收/已完成/不做/规范与决策；根备注为工作循环与分工（Bug/版本不入图）
- [ ] 想法池说明含 ::flask 图标约定（探索/讨论中打标，摘标即成熟）；想法分支备注自包含（复制子树给 AI 即上下文）
